name: Lint

on: [push, pull_request]

jobs:
  fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ziglang/actions/setup-zig@v1
        with:
          version: 0.15.0
      - run: zig fmt --check src/ tests/
